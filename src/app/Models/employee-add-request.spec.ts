import { EmployeeAddRequest } from './employee-add-request';

describe('EmployeeAddRequest', () => {
  it('should create an instance', () => {
    expect(new EmployeeAddRequest()).toBeTruthy();
  });
});
